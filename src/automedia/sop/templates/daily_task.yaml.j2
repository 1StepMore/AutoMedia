# Daily tasks for {{ brand }} — {{ date }}
date: "{{ date }}"
brand: {{ brand }}
tasks:
{% for task in tasks %}
  - "{{ task }}"
{% endfor %}
{% if overrides %}
overrides:
{% for key, value in overrides.items() %}
  {{ key }}: {{ value }}
{% endfor %}
{% endif %}
