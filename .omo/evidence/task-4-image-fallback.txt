Task 4: LLM-generated image prompts — Fallback Verification
============================================================
Date: 2026-07-13
File: src/automedia/pipelines/image_pipeline.py

Verification: _generate_image_prompt() fallback on LLM failure
Command: python3 -c "from automedia.pipelines.image_pipeline import _generate_image_prompt; p=_generate_image_prompt('AI robots in healthcare', 'MedTech', 2); assert 'body image' in p; assert '3' in p"

Result: PASS
  - No API key available → LLMError raised → gracefully caught
  - Warning logged: "LLM image prompt generation failed ... — using fallback"
  - Fallback returns: "AI robots in healthcare body image 3"
  - Naive template (f"{topic} body image {image_index + 1}") is ONLY in fallback path

Grep verification (naive template NOT in primary path):
  $ grep -n "f.*topic.*body image" src/automedia/pipelines/image_pipeline.py
  Line 203 (docstring reference)
  Line 251 (fallback return only)
